import ConsolidatedReport from '../../_components/reports/consolidated-report';

export const metadata = { title: 'ConsolidatedReport' };

export default function Page() {
  return <ConsolidatedReport />;
}
