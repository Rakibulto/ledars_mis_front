import FiscalPositions from '../../_components/configuration/fiscal-positions';

export const metadata = { title: 'FiscalPositions' };

export default function Page() {
  return <FiscalPositions />;
}
