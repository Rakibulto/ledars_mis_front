import CurrencyExchangeRates from '../../_components/configuration/currency-exchange-rates';

export const metadata = { title: 'CurrencyExchangeRates' };

export default function Page() {
  return <CurrencyExchangeRates />;
}
