import DeferredRevenue from '../../_components/transactions/deferred-revenue';

export const metadata = { title: 'DeferredRevenue' };

export default function Page() {
  return <DeferredRevenue />;
}
