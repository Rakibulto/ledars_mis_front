import DeferredExpenses from '../../_components/transactions/deferred-expenses';

export const metadata = { title: 'DeferredExpenses' };

export default function Page() {
  return <DeferredExpenses />;
}
